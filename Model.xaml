<Window x:Class="Projet_Mines_Official.Model"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projet_Mines_Official"
        mc:Ignorable="d"
          WindowStartupLocation="CenterScreen" 
        WindowStyle="None" 
        Title="" Height="180" Width="350">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock  Grid.Row="0" Grid.ColumnSpan="3" Grid.RowSpan="2" FontSize="16" 
         Text="Are you sure you want me to create a model?"
         TextWrapping="Wrap" TextAlignment="Justify" Margin="20,25,0,0" FontFamily="Arial" 
                    FontWeight="Bold" Foreground="#0861AD"/>
        <Button  Grid.Row="3" Grid.Column="2" Grid.ColumnSpan="2" FontSize="16"  Content="OK" Width="100" Background="#EAA221"/>
       


    </Grid>
</Window>
